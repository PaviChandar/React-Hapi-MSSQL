import { createContext } from "react";

export const LoginContext = createContext<any>({})

//class-comp - context - upd

//forms - multiple state - controlled and uncontrolled comp - 10inputs - state handling - class and func